- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc:
     ~/.gitconfig:
- create:
    - ~/Work

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
   - echo "Hey whats up guys its deez nuts"
   - sleep 2
   - 
      command: ./test.zsh
      stdout: true
      stderr: ture
